newStr = "";
newStr1 = "";

for(var i = 0; i < str.length; i++){
  if(str[i] == " "){
    if(str[i + 1] != " "){
      newStr += str[i +1]
    }
  }
}
for(var x = i; x < str.length; x++);
  if(str[x] == " "){
    if(str[x + 1]) != " ")
    newStr
  }
